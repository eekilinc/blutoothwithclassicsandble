name: bt_dual_bluetooth
description: Dual Bluetooth Controller (Classic + BLE)
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_blue_plus: ^1.31.15       # BLE
  bluetooth_classic: ^0.3.2         # Classic
  flutter_joystick: ^1.2.0          # Joystick widget

flutter:
  uses-material-design: true
